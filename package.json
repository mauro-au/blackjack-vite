{
  "name": "03-blackjack-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "postbuild": "copyfiles -u 1 dist/**/* docs",
    "deploy": "npm run build && npm run postbuild"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "vite": "^6.3.5"
  },
  "dependencies": {
    "underscore": "^1.13.7"
  }
}
